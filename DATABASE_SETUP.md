# إعداد قاعدة البيانات

## الخيار المقترح

استخدم Supabase أو أي PostgreSQL متاح. بعد إنشاء قاعدة البيانات، نحتاج قيمة:

```text
DATABASE_URL
```

## ملف البيئة المحلي

أنشئ ملف `.env.local` أو حدّث الموجود:

```text
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres
DATABASE_SSL=true
```

إذا كانت قاعدة البيانات محلية بدون SSL:

```text
DATABASE_SSL=false
```

## إنشاء الجداول

```bash
npm run db:init
```

## إنشاء الجداول مع بيانات بداية

```bash
npm run db:init:seed
```

## تشغيل محلي مع API

بعد إضافة `DATABASE_URL` يمكن تشغيل التطبيق والـ API محليا من رابط واحد:

```bash
npm start
```

الرابط الافتراضي:

```text
http://localhost:4174/index.html
```

بيانات البداية تشمل:

- مستخدمين مبدئيين: بندر، ابو فايز، عمر، صدام.
- الحساب الرسمي.
- الحساب الفرعي.
- كاش.
- تنبيهات موظفين وسيارات مبدئية قابلة للتعديل.

## أكواد الدخول المبدئية

هذه أكواد مؤقتة للتجربة فقط، ويجب تغييرها لاحقا:

```text
بندر: 1111
ابو فايز: 2222
عمر: 3333
صدام: 4444
```

## بعد النشر على Vercel

أضف المتغيرات نفسها في إعدادات المشروع:

```text
DATABASE_URL
DATABASE_SSL
```

بعدها سيبدأ `/api/app` باستخدام قاعدة البيانات، وستصبح إعدادات دفترة والحركات المالية محفوظة مركزيا.

## الدومين

يفضل نشر التطبيق على رابط فرعي مستقل عن الموقع التسويقي:

```text
app.wkaimah.com.sa
```

تفاصيل الربط موجودة في `DEPLOYMENT_PLAN.md`.
