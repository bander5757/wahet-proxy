# خطة نشر تطبيق واحة الخيمة

## الدومين

الموقع الحالي يبقى كما هو على الدومين الرئيسي. التطبيق يفضل أن يكون على رابط فرعي مستقل:

```text
app.wkaimah.com.sa
```

بدائل مناسبة:

```text
admin.wkaimah.com.sa
dashboard.wkaimah.com.sa
```

## طريقة الربط المقترحة

1. نشر المشروع على Vercel.
2. إضافة الرابط الفرعي في إعدادات المشروع داخل Vercel.
3. من لوحة DNS للدومين، إضافة سجل:

```text
Type: CNAME
Name: app
Value: cname.vercel-dns.com
```

قد يختلف السجل حسب التعليمات التي يعطيها Vercel عند إضافة الدومين.

## ما يلزم قبل فتحه للأعضاء

- ربط قاعدة بيانات PostgreSQL أو Supabase.
- إضافة متغيرات البيئة في Vercel:

```text
DATABASE_URL
DATABASE_SSL
```

- تشغيل سكربت إنشاء الجداول والبيانات الأولية:

```bash
npm run db:init:seed
```

- تغيير أكواد الدخول المبدئية قبل الاستخدام الفعلي.
- إدخال إعدادات دفترة من حساب بندر فقط.

## كتطبيق جوال

تم تجهيز التطبيق كـ PWA:

- اسم التطبيق: واحة الخيمة.
- يعمل من المتصفح على الجوال واللابتوب.
- يمكن إضافته إلى الشاشة الرئيسية من Safari أو Chrome.
- يتم تحميل الواجهة الأساسية بسرعة عبر service worker.

## ملاحظة مهمة

الحفظ المركزي بين أجهزة بندر، ابو فايز، عمر، وصدام يعتمد على قاعدة البيانات. بدون قاعدة البيانات سيبقى بعض الحفظ محليا على نفس الجهاز والمتصفح فقط.
